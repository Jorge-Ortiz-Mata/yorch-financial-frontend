import { Text } from "react-native";

const CustomTextLabel = ({title}) => {

  return(
    <Text className="text-white font-bold text-base mb-2">{title}</Text>
  )
}

export default CustomTextLabel;
