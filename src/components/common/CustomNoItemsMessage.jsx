import { Text } from "react-native"

const CustomNoExpensesMessage = ({ title }) => {

  return(
    <Text className="text-white text-center font-semibold text-base my-5">{title}</Text>
  )
}

export default CustomNoExpensesMessage;
