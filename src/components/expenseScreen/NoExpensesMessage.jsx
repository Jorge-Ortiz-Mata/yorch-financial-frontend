import { View, Text } from "react-native"

const NoExpensesMessage = () => {

  return(
    <Text className="text-white text-center font-semibold text-base my-5">Aún no tienes gastos guardados.</Text>
  )
}

export default NoExpensesMessage;
